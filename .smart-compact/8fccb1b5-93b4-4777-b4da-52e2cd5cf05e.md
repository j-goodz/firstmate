# Smart Compact Context (recovered)
2026-09-22 05:52

## Current Task
Review the branch change (fm/cache-excl, da9607d..7fb64d9) that adds a `NEXUS_CACHE_MODE=off` export to `bin/fm-spawn.sh` so fanned-out worker sessions are never auto-cycled by nexus's session cycler while the long-lived brain session keeps auto-cycling.
Next: Answer F1 (should a persistent `--secondmate` also be excluded from auto-cycling, or should the export be gated on `KIND != secondmate`?) and mechanically fix F2 (make the new test fail if the launch-level export at `bin/fm-spawn.sh:4696` is deleted).

## Files Touched This Session
bin/fm-spawn.sh, tests/fm-spawn-cache-mode-excl.test.sh

## Key Decisions Made
The exclusion is delivered via `NEXUS_CACHE_MODE=off` (a real nexus control: `cache_policy.resolve` precedence session > env > project > global, exact `auto|manual|off` match; any non-`auto` mode yields `action: none`), exported both in the pane pre-launch channel and inside `LAUNCH`; the in-`LAUNCH` export survives the `env -i` allowlist posture even though the name is not on the floor list; the captain's own session is left untouched so the brain keeps cycling.

## Errors Encountered
None encountered during review; the change is functionally correct. Two gaps found: F1 unconditional `off` also silences a persistent secondmate (intent ambiguity), F2 the new test replays pane exports before running the launch, so deleting the launch-level export at line 4696 still passes both tests.

## Pending Work
F2 test hardening (add a launch-command-only case mirroring `test_launch_command_carries_the_switch_without_the_pane_export`); F3 optional doc paragraph for the new guarantee analogous to `docs/configuration.md:443`.

## Git State
Branch: 



7fb64d9 fix(bin): exclude every fanned-out worker from the nexus session cycler
da9607d fix(bin): allow cleanup of windowless legacy task records (#5236)
3fcbc6c fix(bin): retain Pi watcher predecessor to stop false down alarms (#5174)
8c1cdb7 ci: pin the no-mistakes required check to v1.80.1 (#5195)
aec043c test: fix Claude session-start drain live E2E (#5165)

